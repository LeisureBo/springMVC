<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 注意节点顺序 (properties?,settings?,typeAliases?,typeHandlers?,objectFactory?,objectWrapperFactory?,plugins?,environments?,databaseIdProvider?,mappers?)-->
	<!-- 配置映射类的别名 -->
	<typeAliases>
		<typeAlias alias="User" type="com.bo.model.User"/>
	</typeAliases>
 	<!-- 配置Mapper文件的路径 -->
	<mappers>
		<mapper resource="com/bo/mapper/UserMapper.xml"/>
	</mappers>

</configuration>